# build / runtime artifacts (never committed)
CTranslate2/
venv/
dist/
*.whl
__pycache__/
*.pyc

# downloaded / generated audio
sample.wav
sample_long.wav

# logs + phase markers
*.log
install.done
install.failed
build.done
build.failed
bench.done
bench.failed
cuda-env.sh

# internal QA / drafts — not for the public repo
code_review.md
DRAFT_upstream_comment.md

# NOTE: benchmark_results.json and validation_results.json ARE committed as
# evidence backing the README numbers.
